# Memory — Analysis Squad (chung)

> Memory riêng từng con ở `a1-curve/`, `a2-insight/`, `a3-forecast/`.
> File này giữ bài học chung cho cả nhóm phân tích.

## Mối quan hệ macro ↔ swap đã xác nhận
*(vd: "SBV hút tín phiếu → O/N tăng → swap points ngắn hạn dương hơn" — ghi khi có bằng chứng)*

- **[01/07/2026] Chốt quý → chân ngắn curve, KHÔNG chạm chân dài:** window-dressing BCTC đẩy ON lên đỉnh **đúng ngày chốt** (30/06: 9.50%) rồi **xả trong 1 phiên** (01/07: 3.75%); 3M–1Y bất động. → Đọc curve quanh cuối quý: bướu/spike đầu cong = mùa vụ, đừng nhầm là dịch chuyển kỳ vọng.
- **[01/07/2026] Khi i_VND hội tụ i_USD (SOFR) → swap points co về ~0 toàn curve.** ON 3.75 ≈ SOFR 3.62 → forward ≈ spot, carry trung tính. Ngược lại 23/06 ON 1.65 << SOFR → forward discount lớn. Quy tắc nhanh: theo dõi spread (ON_VND − SOFR) như chỉ báo swap points chân ngắn.

## Nhận định sai của nhóm & nguyên nhân
*(quan trọng nhất để học — phối hợp với sổ lỗi ở memory.md root)*

## Cách phối hợp A1→A2→A3 hiệu quả
*(ghi khi tìm ra cách bàn giao tốt giữa 3 con)*
